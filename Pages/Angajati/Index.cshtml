@page
@model Istrate_Alexandra_Proiect.Pages.Angajati.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Lista angajatilor</h1>

<p>
    <a asp-page="Create">Introduceti un nou angajat</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Angajat[0].NumePrenumeAngajat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Angajat) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumePrenumeAngajat)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Editare</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Detalii</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Stergere</a>
            </td>
        </tr>
}
    </tbody>
</table>
